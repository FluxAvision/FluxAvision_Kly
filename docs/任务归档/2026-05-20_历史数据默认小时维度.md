# 任务归档 - 历史数据页面默认维度改为小时，去掉各设备分栏

**日期：** 2026-05-20
**来源：** 小虾秘
**Commit：** bf519cc

## 改动内容

### 1. 默认维度改为小时
- rontend-vue/src/components/history/HistoryData.vue
- dimension = ref<string>('day') → dimension = ref<string>('hour')
- 页面加载后默认选中「小时」维度，日期为当天

### 2. 天维度默认范围改为30天
- 在 switchDimension 中新增 dim === 'day' 分支
- 当起止日期相同时（切换到天维度），startDate 设为 29 天前
- 其他维度（周/月/年）的默认范围保持不变

### 3. 删除各设备客流分栏 Panel
- 删除 template 中 deviceDailyData 整个区块
- 删除 DeviceDailyData interface
- 删除 deviceDailyData ref 及相关逻辑（dailyDeviceData 解析）
- 删除 isSingleDay computed（仅被该区块使用）
- 删除 Camera 图标导入（仅被该区块使用）

### 4. 后端维度支持
- ackend/routers/traffic_history.py — 增加 dimension 参数支持
- 支持 hour/day/week/month/year 聚合查询

## 验收
- [x] TypeScript 类型检查通过
- [x] Vite 生产构建成功
- [x] Git 已提交并推送

## 文件变更
- rontend-vue/src/components/history/HistoryData.vue — 全面重写
- ackend/routers/traffic_history.py — 增加维度查询
