# 2026-05-20 — 仪表盘页面布局调整

## 任务
将仪表盘页面下方的「各设备今日客流」和「设备状态」两个 Panel 从上下叠放改为左右并排显示。

## 改动
- **文件：** `frontend-vue/src/components/dashboard/Dashboard.vue`
- **改动：** 外层容器 `flex flex-col lg:flex-row` → `grid grid-cols-1 lg:grid-cols-2`
- 移除子元素的 `flex-1`（grid 不再需要）
- 内部内容和样式不变

## 验收
- TypeScript 类型检查 ✅
- Vite 生产构建 ✅
- Git 提交：`8ac0cd7` feat: 仪表盘各设备客流与设备状态并排显示

## 备注
- 第一个任务，走的是旧的（错误的）沟通流程完成
- 后续新任务将采用「只干活不回复」模式
